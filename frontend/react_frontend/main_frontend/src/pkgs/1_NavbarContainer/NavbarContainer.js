// css
import "./assets/css/NavbarContainer.css"

// 패키지 파일
import { Routes } from "react-router-dom"

// 컴퍼넌트

export const NavbarContainer = () => {
  return (
    <div className = "navbarContainer">
      <Routes>


      </Routes>
    </div>
  )
}